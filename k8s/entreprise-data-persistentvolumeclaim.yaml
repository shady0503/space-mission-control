apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: entreprise-data
  name: entreprise-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
