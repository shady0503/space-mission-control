apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: spacecraft-data
  name: spacecraft-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
