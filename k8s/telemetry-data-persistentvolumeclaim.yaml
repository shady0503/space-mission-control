apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: telemetry-data
  name: telemetry-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
